import { ToColumnMoveDirective } from './to-column-move.directive';

describe('ToColumnMoveDirective', () => {
  it('should create an instance', () => {
    const directive = new ToColumnMoveDirective();
    expect(directive).toBeTruthy();
  });
});
