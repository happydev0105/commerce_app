import { SplashScreenDirective } from './splash-screen.directive';

describe('SplashScreenDirective', () => {
  it('should create an instance', () => {
    const directive = new SplashScreenDirective();
    expect(directive).toBeTruthy();
  });
});
